# Known faces

Place one clear face image per known person in this directory.
The filename becomes the published person's name.

Examples:

- `John.jpg` -> publishes `John`
- `Alice.png` -> publishes `Alice`

Do not commit personal face images to a public repository unless you intend to publish them.
